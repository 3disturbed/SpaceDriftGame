let fuel = 100;

function createFuelPickup() {
  // Similar to asteroids, create a fuel pickup that falls from the top
}

function checkCollisionWithFuel() {
  // Check if spaceship collides with fuel
  // If so, increase fuel
}
